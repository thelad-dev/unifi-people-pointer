{
  "config": {
    "step": {
      "user": {
        "title": "UniFi People Pointer Setup",
        "description": "Host = controller LAN IP or hostname (e.g. 192.168.88.1). API token from UniFi OS → Settings → Control Plane → Integrations. Local UniFi uses a self-signed cert — SSL verification is off by default.",
        "data": {
          "host": "UniFi Controller host",
          "api_token": "API token",
          "verify_ssl": "Verify SSL certificate (only if you use a trusted public hostname/cert)"
        }
      },
      "reauth_confirm": {
        "title": "Reauthenticate UniFi People Pointer",
        "description": "Enter a new UniFi API token",
        "data": {
          "token": "API token"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to UniFi Controller",
      "invalid_auth": "Invalid authentication",
      "invalid_host": "Invalid host (use an IP address or hostname)",
      "timeout": "Connection to UniFi Controller timed out",
      "unknown": "Unexpected error occurred",
      "ssl_error": "SSL certificate error. For a local controller/IP leave “Verify SSL certificate” disabled (default)."
    },
    "abort": {
      "already_configured": "UniFi People Pointer is already configured",
      "reauth_successful": "Reauthentication successful"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "UniFi People Pointer Options",
        "description": "Configure polling and event settings",
        "data": {
          "scan_interval": "Scan interval (seconds)"
        }
      }
    }
  }
}
